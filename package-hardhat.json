{
  "name": "nft-artist-dapp-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for NFT Artist DApp",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy-nft-minter.js --network monadTestnet",
    "verify": "hardhat verify --network monadTestnet",
    "test": "hardhat test",
    "node": "hardhat node"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "hardhat": "^2.19.0",
    "dotenv": "^16.3.1"
  }
}